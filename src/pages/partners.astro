---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
---

<Layout>
  <!-- Hero Section -->
  <section class="hero-gradient relative overflow-hidden">
    <div class="absolute inset-0 bg-mesh opacity-30"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 md:py-28 text-center">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-4">Our Partners</h1>
      <p class="text-lg md:text-xl text-primary-200 max-w-2xl mx-auto">
        Collaborations for Success
      </p>
    </div>
  </section>

  <!-- Partners Content -->
  <section class="min-h-screen bg-mesh">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24">

      <!-- Intro Text -->
      <div class="max-w-3xl mx-auto text-center mb-16">
        <p class="text-lg md:text-xl text-gray-700 leading-relaxed">
          At X4O, we believe in the power of collaboration. Our partnerships enable us to deliver
          comprehensive solutions that drive real results for our clients.
        </p>
      </div>

      <!-- Partner Logos Showcase -->
      <div class="glass-card rounded-2xl p-8 md:p-12 mb-12">
        <h2 class="text-2xl md:text-3xl font-bold text-primary-dark text-center mb-4">
          Our Trusted Partners
        </h2>
        <p class="text-gray-500 text-center mb-10">
          We work with leading organizations across South Africa
        </p>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 md:gap-10 max-w-4xl mx-auto">
          <!-- EdMeCa -->
          <div class="flex flex-col items-center justify-center p-6 rounded-xl bg-white/70 hover:bg-white/90 transition-colors shadow-sm">
            <img src="/images/EdMeCa_logo.png" alt="EdMeCa" class="h-24 w-auto object-contain mb-4" />
            <p class="text-primary-dark font-semibold text-center">EdMeCa</p>
          </div>

          <!-- IDC -->
          <div class="flex flex-col items-center justify-center p-6 rounded-xl bg-white/70 hover:bg-white/90 transition-colors shadow-sm">
            <img src="/images/idc-logo.png" alt="IDC" class="h-24 w-auto object-contain mb-4" />
            <p class="text-primary-dark font-semibold text-center">IDC</p>
          </div>

          <!-- Mzilikazi -->
          <div class="flex flex-col items-center justify-center p-6 rounded-xl bg-white/70 hover:bg-white/90 transition-colors shadow-sm">
            <img src="/images/mzilikazi_logo.png" alt="Mzilikazi" class="h-24 w-auto object-contain mb-4" />
            <p class="text-primary-dark font-semibold text-center">Mzilikazi</p>
          </div>
        </div>
      </div>

      <!-- CTA Section -->
      <div class="glass-card rounded-2xl p-8 md:p-12 text-center">
        <h2 class="text-2xl md:text-3xl font-bold text-primary-dark mb-4">
          Interested in partnering with us?
        </h2>
        <p class="text-gray-600 text-lg mb-8 max-w-2xl mx-auto">
          We are always looking for new partnerships that create mutual value. Let us explore how we can work together.
        </p>
        <a href="/contact" class="btn-primary text-lg">
          Get in Touch
        </a>
      </div>

    </div>
  </section>
</Layout>
